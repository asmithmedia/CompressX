{
  "name": "compressx",
  "version": "0.1.0",
  "description": "Compress LLM models for local deployment. GGUF quantization for Ollama, llama.cpp, LM Studio.",
  "type": "module",
  "bin": {
    "compressx": "./bin/compressx.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx bin/compressx.ts"
  },
  "keywords": ["llm", "compression", "gguf", "ollama", "quantization", "llama-cpp", "ai", "model"],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.0",
    "inquirer": "^9.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "tsx": "^4.0.0",
    "@types/node": "^20.0.0"
  }
}
